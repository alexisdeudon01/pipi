version: '3.8'
services:
  app:
    build: .
    environment:
      - PYTHONUNBUFFERED=1
      - DISPLAY=${DISPLAY}
    volumes:
      - ./app:/app/app
      - /tmp/.X11-unix:/tmp/.X11-unix
    command: python app/gui.py
